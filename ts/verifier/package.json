{
  "name": "canton-solvency-verifier",
  "version": "0.1.0",
  "description": "Browser-side verifier for Canton proof-of-solvency commitments (Merkle sum tree)",
  "license": "Apache-2.0",
  "repository": "github:Rocky-exchange/canton-proof-of-solvency",
  "type": "module",
  "main": "src/verify.ts",
  "scripts": {
    "test": "vitest run",
    "build:offline": "node scripts/build-offline.mjs",
    "emit:statement": "node scripts/emit-statement.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "ajv": "^8.20.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.5.0",
    "vitest": "^2.0.5"
  }
}
