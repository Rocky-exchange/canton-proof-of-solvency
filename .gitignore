target/
node_modules/
dist/
.DS_Store
target/package/
.daml/

# esbuild scratch for the compatibility-statement emitter
ts/verifier/src/.corpus.bundle.mjs

# Canton runtime logs. These were tracked, so any local `daml start` left the
# working tree dirty and blocked branch switches, and every run rewrote files
# nobody reads in review.
daml/solvency-anchor/log/
daml/**/.daml/
